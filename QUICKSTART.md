# 🚀 Quick Start Guide - แก้ไขปัญหาเร่งด่วน

## ⚡ แก้ไขเร็ว 3 ขั้นตอน

### 1️⃣ อัปเดตโค้ด (5 นาที)

```bash
# อัปโหลด app.py ใหม่ไปที่ Git repository ของคุณ
git add app.py
git commit -m "Fix: Support both AssessRisk and AssessPersonalRisk intents"
git push origin main
```

### 2️⃣ ตั้งค่า Environment Variables ใน Render (5 นาที)

ไปที่ Render Dashboard > Your Web Service > Environment Tab

เพิ่ม Variables ต่อไปนี้:

```
CHANNEL_ACCESS_TOKEN = <YOUR_LINE_CHANNEL_TOKEN>
NURSE_GROUP_ID = <YOUR_LINE_GROUP_ID>
GSPREAD_CREDENTIALS = <YOUR_GOOGLE_JSON_CREDENTIALS>
```

💡 **ดูวิธีหาค่าเหล่านี้ได้ที่ `SETUP_GUIDE.md`**

### 3️⃣ ทดสอบ (2 นาที)

ส่งข้อความใน LINE:
```
ประเมินความเสี่ยง อายุ 65 น้ำหนัก 98 ส่วนสูง 165 เป็นเบาหวาน
```

ควรได้รับคำตอบพร้อมผลประเมิน + การแจ้งเตือนใน LINE Group

---

## 🔍 ตรวจสอบว่าแก้ไขสำเร็จหรือไม่

### ใน Render Logs ควรเห็น:

```
✅ Intent incoming: AssessRisk user=xxx params={"age": 65.0, ...}
✅ Profile Saved
✅ Push Notification Sent to nurse group
✅ POST /webhook HTTP/1.1 200
```

### ไม่ควรเห็น:

```
❌ WARNING LINE token or NURSE_GROUP_ID not configured
❌ Unhandled intent: AssessRisk
❌ POST /webhook HTTP/1.1 404
```

---

## 🆘 ถ้ายังไม่ได้

### Problem 1: ยังได้ "บอทไม่เข้าใจคำสั่งนี้"

**สาเหตุ**: โค้ด app.py ยังไม่อัปเดต

**แก้ไข**:
1. ตรวจสอบว่า Git push สำเร็จแล้วหรือยัง
2. ตรวจสอบใน Render > Settings > Build & Deploy ว่า deploy ล่าสุดเสร็จแล้ว
3. ลอง Manual Deploy: Render Dashboard > Manual Deploy > Deploy latest commit

---

### Problem 2: ไม่มี LINE Notification

**สาเหตุ**: Environment Variables ยังไม่ได้ตั้ง

**แก้ไข**:
1. ไปที่ Render > Environment
2. ตรวจสอบว่ามี `CHANNEL_ACCESS_TOKEN` และ `NURSE_GROUP_ID` หรือยัง
3. ถ้ายังไม่มี ให้เพิ่มเข้าไป
4. กด Save Changes (Render จะ auto-redeploy)

---

### Problem 3: Logs แสดง 404 หรือ Error อื่นๆ

**แก้ไข**:
1. รันคำสั่งทดสอบ:
   ```bash
   curl https://kwannurse-bot.onrender.com/
   ```
   ควรได้ `{"status": "ok"}`

2. ถ้ายัง 404 แสดงว่า app.py ยังไม่อัปเดต
   - ลอง Clear Build Cache ใน Render
   - Manual Deploy อีกครั้ง

---

## 📋 Checklist ก่อนปิดงาน

- [ ] app.py อัปเดตแล้ว (ตรวจสอบใน Git)
- [ ] Deploy สำเร็จใน Render (ไม่มี Build Errors)
- [ ] Environment Variables ตั้งครบ 3 ตัว
- [ ] Health check ทำงาน (`curl https://your-url.onrender.com/`)
- [ ] ทดสอบส่งข้อความใน LINE ได้รับคำตอบถูกต้อง
- [ ] ได้รับ LINE Notification ใน Nurse Group (สำหรับ High Risk)
- [ ] Logs ไม่มี WARNING หรือ ERROR

---

## 🎯 สิ่งที่เปลี่ยนไป

### Before (เดิม):
```python
elif intent == 'AssessPersonalRisk':  # ❌ รองรับแค่ชื่อนี้
    params.get('disease')  # ❌ รองรับแค่ parameter นี้
```

### After (ใหม่):
```python
elif intent == 'AssessPersonalRisk' or intent == 'AssessRisk':  # ✅ รองรับทั้ง 2 ชื่อ
    params.get('disease') or params.get('diseases')  # ✅ รองรับทั้ง 2 parameter
```

---

## 📞 ต้องการความช่วยเหลือเพิ่มเติม?

อ่านเอกสารแบบละเอียดได้ที่:
- `SETUP_GUIDE.md` - คู่มือแก้ไขปัญหาแบบละเอียด
- `CHANGELOG.md` - รายละเอียดการเปลี่ยนแปลงทั้งหมด
- `.env.template` - ตัวอย่าง Environment Variables

---

**เวลาที่ใช้ทั้งหมด: ~15 นาที**  
**ระดับความยาก: ⭐⭐☆☆☆ (Easy)**
